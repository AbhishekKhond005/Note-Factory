# ── Dev profile: H2 in-memory (no Postgres needed) ────────────────────────────
spring.datasource.url=jdbc:h2:mem:notefactory;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL;NON_KEYWORDS=VALUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=none
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Use H2-compatible migration scripts
spring.flyway.locations=classpath:db/migration/h2

# ── Storage paths ──────────────────────────────────────────────────────────────
notefactory.storage.notes-dir=/home/abhishek/Coding/RESUME PROJECTS/Note-Factory/notes
notefactory.storage.roadmaps-dir=/home/abhishek/Coding/RESUME PROJECTS/Note-Factory/roadmaps
